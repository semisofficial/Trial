# Optional direct backend API URL. Render production uses the same-origin /api
# rewrite from render.yaml, so leave this unset there. Local development falls
# back to http://localhost:5000/api.
VITE_API_URL=https://semiskitchenbe.onrender.com/api
